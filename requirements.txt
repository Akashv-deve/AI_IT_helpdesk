# Runtime dependencies. Upper bounds keep a future major release from
# silently breaking a clone months from now.
langgraph>=0.2,<2.0
langchain-core>=0.3,<2.0
langchain-ollama>=0.2,<2.0
mcp>=1.0,<3.0
rich>=13.0,<16.0
python-dotenv>=1.0,<2.0

# Local web UI. Streamlit pulls in pandas, which the ticket and history
# tables use for rendering.
streamlit>=1.36,<2.0
pandas>=2.0,<3.0
